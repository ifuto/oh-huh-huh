# suifu-island_1000x1000.glb 診断レポート

> **【2026-09-11 更新】修復完了。** `island.tsx`(プロシージャル生成コード)が提供されたため、
> three@0.186.0 + GLTFExporter でヘッドレス再生成を実施し、壊れたGLBを正常なものに差し替え済み。
> 詳細はこのファイル末尾の「修復記録」を参照。


## 結論

**ファイル自体が破損しています。** GLB としての構造は残っていますが、バイナリデータがどこかの段階で
**テキスト(UTF-8)として解読 → 再保存** されています。その際、UTF-8 として不正なバイトがすべて
置換文字 `U+FFFD`(バイト列 `EF BF BD`)に置き換わり、**頂点データ(ジオメトリ)の約 35% が失われています。
元のバイナリはこのコピーからは復元できません。**

## 証拠

### 1. ヘッダの長さフィールドが壊れている

GLB ヘッダ(先頭 12 バイト + チャンクヘッダ)に、本来存在しないはずの `EF BF BD` が入っています。

| オフセット | 内容 | 本来の値(逆算) | 破損後 |
|---|---|---|---|
| 8–11 | ファイル全体の長さ | `54 F2 5C 00` (6,091,348) | `54 [FFFD] 5C 00` |
| 12–15 | JSONチャンク長 | `80 58 00 00` (22,656) | `[FFFD] 58 00 00` |
| ~22680 | BINチャンク長 | `B8 99 5C 00` (6,068,664) | `[FFFD][FFFD] 5C 00` |

`0xF2, 0x80, 0xB8, 0x99` はいずれも UTF-8 の 1 バイト目として不正なバイトで、
テキスト変換されるとちょうど `EF BF BD` になります。残りのバイト(`54 5C 00 58 00 00 ...`)は
完全に一致しており、この逆算は確定的です。

### 2. 置換文字が 207 万個埋め込まれている

| 項目 | 値 |
|---|---|
| ファイル内の `U+FFFD` 数 | **2,068,311 個** |
| 元のファイルサイズ(逆算) | 6,091,348 バイト |
| 現在のファイルサイズ | 10,187,746 バイト(+4.1MB) |
| BINチャンク(ジオメトリ) | 6,068,664 バイト |
| このうち消失したバイト | **約 2,108,535 バイト(≈ 34.7%)** |

### 3. 破損していない部分

- **JSONチャンクは完全に無傷**(strict UTF-8 / strict JSON ともにパース成功、置換文字ゼロ)
  - ノード 35 / メッシュ 29 / マテリアル 21 / accessor・bufferView 各 60
  - 全 accessor の componentType は 5126 (float) で整合
  - テクスチャ・画像は含まれていない(0 枚)
- つまり「設計情報(シーン構造・マテリアル定義)」は残っているが、**実体の数値データが 1/3 欠けている** 状態

## 原因(推定)

バイナリをテキストとして読み書きしたときに発生する典型的な破損です。考えられる経路:

- テキストエディタ(VS Code 等)で開いてそのまま保存した
- `FileReader.readAsText()` で読んで保存した
- Python(`open(..., 'r'/'w')` テキストモード)や Node(`fs.readFileSync(p, 'utf-8')`)で読み書きした
- テキストとして処理されるアップロード/転送経路(チャット添付・クリップボード経由など)を通った

なお、git 履歴の最初のコミット("Add files via upload")の時点で既にこの状態のため、
**GitHub にアップロードする前(手元のファイルの段階)で既に壊れています。**

## 対処法

修復は不可能です(置換された元バイトの情報はどこにも残っていないため)。次のいずれかで再取得してください:

1. **再エクスポート**: 元の three.js プロジェクト(generator: `THREE.GLTFExporter r186`)から
   `GLTFExporter` で glTF Binary (.glb) を出し直す
2. **原本を探す**: 破損前の .glb が残っている端末・バックアップから入手する
3. 再取得後の転送は必ず**バイナリ安全な方法**で(git / 通常のファイル添付 / scp 等。テキスト変換が
   入る経路は避ける)

取得したら `git` でコミットし直せば、このリポジトリの差し替えはすぐできます。


---

## 修復記録(2026-09-11)

### 方法
`island.tsx` の `createIsland()`(シード固定の完全決定論的コード)を入手できたため、
サンドボックス内の Node.js + three@0.186.0(元のエクスポーターと同一バージョン)で
`IslandViewer.exportGLB()` と同一の変換を再実行し、GLBを再生成した。

### 検証結果(すべて合格)

| 項目 | 結果 |
|---|---|
| ヘッダ / チャンク構造 | 正常(総長・パディング・BIN長すべて整合) |
| U+FFFD 置換文字 | **0 個** |
| accessor/bufferView 範囲 | 全 60 件が範囲内、NaN なし、min/max 宣言値と一致 |
| 頂点数 | **244,221**(破損ファイルのJSONが宣言していた元の頂点数と完全一致) |
| BINチャンク | **6,068,664 バイト(元と同一サイズ)** |
| シーンJSON | 元のJSON(破損ファイルから救出)と**マテリアル5箇所の最終桁1ULP差以外完全一致** |
| ファイルサイズ | 6,091,352 バイト(逆算元サイズ 6,091,348 と +4B、JSON内 float 文字列表記差によるもの) |

1ULPの差は three のバージョン間での Color 変換の丸め差と考えられ、見た目・構造への影響はなし。
レンダリング確認(上面・斜め上部)でも浮遊島(芝生・湖・滝・小径・植生・建物)が正常に描画された。

### 教訓(再発防止)
- GLBなどバイナリファイルをテキストエディタで開いて保存しない
- コードはテキスト(.ts/.tsx)として受け渡し、**モデルは生成スクリプトからその場で書き出す**のが最も安全
- 今回のようにシード固定のプロシージャル生成なら、コードさえ残れば何度でも完全復元可能
